apiVersion: v1
kind: Pod
metadata:
  name: debug
  labels:
    app: debug
spec:
  # imagePullSecrets:
  #   - name: gitea-pull-secrets
  containers:
  - name: debug
    image: registry1.dso.mil/ironbank/redhat/ubi/ubi9:9.6@sha256:363f765fdc6fc8f1dba710341b099af0b2e55335cad155041e4364dc7a99be0f 
    command: ["/bin/bash", "-c", "sleep infinity"]
    securityContext:
      runAsUser: 0
      runAsGroup: 0
    # resources:
    #   requests:
    #     memory: "64Mi"
    #     cpu: "250m"
    #   limits:
    #     memory: "128Mi"
    #     cpu: "500m"
    # volumeMounts:
    #   - name: data-vol
    #     mountPath: /data
  restartPolicy: Never
  # volumes:
  #   - name: data-vol
  #     persistentVolumeClaim:
  #       claimName: pgdata-postgres-0
