---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: ersatztv
  name: ersatztv
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ersatztv
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: ersatztv
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: kubernetes.io/hostname
                    operator: In
                    values:
                      - marcus-server
      containers:
        - env:
            - name: TZ
              value: America/Chicago
            - name: NVIDIA_VISIBLE_DEVICES
              value: all
            - name: NVIDIA_DRIVER_CAPABILITIES
              value: all
          image: harbor.yanello.net/ghcr/ersatztv/ersatztv:v26.3.0@sha256:7c402e5f348184359e0951b44d55fb6db3df671fa77fc91dcbd609034d28778b
          imagePullPolicy: IfNotPresent
          name: ersatztv
          resources:
            limits:
              memory: 1Gi
              nvidia.com/gpu: "1"
            requests:
              cpu: 50m
              memory: 100Mi
              nvidia.com/gpu: "1"
          ports:
            - containerPort: 8409
              protocol: TCP
          volumeMounts:
            - name: media
              mountPath: /data/media
            - name: config
              mountPath: /config
          readinessProbe:
            httpGet:
              path: /
              port: 8409
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 10
          livenessProbe:
            httpGet:
              path: /
              port: 8409
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 5
      restartPolicy: Always
      volumes:
        - name: media
          hostPath:
            path: /data/media
        - name: config
          persistentVolumeClaim:
            claimName: config
