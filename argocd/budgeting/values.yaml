---
config:
  db:
    driver: "postgres"
    # PostgreSQL
    host: "db-rw"
    port: "5432"
    name: "budgeting"
    existingSecret: "db-app"
    existingSecretUserKey: user
    existingSecretPasswordKey: password
httpRoute:
  enabled: true
  parentRefs:
    - name: cilium-private-gateway
      namespace: cilium
      sectionName: https
  hostnames:
    - budgeting.yanello.net
