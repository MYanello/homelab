apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ./http-route.yaml

helmCharts:
  - name: chartdb
    repo: oci://harbor.yanello.net/library
    version: "0.1.0"
    releaseName: chartdb
    namespace: chartdb-helm
