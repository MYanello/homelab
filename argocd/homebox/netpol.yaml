---
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: homebox
spec:
  endpointSelector:
    matchLabels:
      app: homebox
  ingress:
    - fromEntities:
        - ingress
      toPorts:
        - ports:
            - port: "7745"
              protocol: TCP
  egress:
    - toCIDR:
        - 10.10.8.0/22
      toPorts:
        - ports:
            - port: "443"
              protocol: TCP
