---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rx-resume
spec:
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: rx-resume
  template:
    metadata:
      labels:
        app: rx-resume
    spec:
      containers:
        - name: rx-resume
          image: harbor.yanello.net/ghcr/amruthpillai/reactive-resume:v5.0.5@sha256:a07258837550b8037f50de0220e6ba0591572789c09f55454bab6189a819c85c
          ports:
            - containerPort: 3010
          env:
            - name: APP_URL
              value: https://resume.yanello.net
            - name: PORT
              value: "3010"
            - name: PUBLIC_URL
              value: "https://resume.yanello.net"
            - name: PRINTER_ENDPOINT
              value: "http://$(PRINTER_SERVICE_HOST):$(PRINTER_SERVICE_PORT)"
            - name: PRINTER_APP_URL
              value: http://rx-resume:3010
            - name: FLAG_DISABLE_SIGNUPS
              value: "false"
            - name: FLAG_DEBUG_PRINT
              value: "true"
            - name: FLAG_DISABLE_EMAIL_AUTH
              value: "true"
            - name: STORAGE_ENDPOINT
              value: "s3.us-west-004.backblazeb2.com"
            - name: STORAGE_REGION
              value: "us-west-004"
            - name: STORAGE_BUCKET
              value: "myanello-rxresume"
            - name: STORAGE_USE_SSL
              value: "true"
            - name: SKIP_BUCKET_CHECK
              value: "false"
            - name: STORAGE_PORT
              value: "443"
            - name: STORAGE_URL
              value: "https://s3.us-west-004.backblazeb2.com/myanello-rxresume"
            - name: OAUTH_PROVIDER_NAME
              value: Authentik
            - name: OAUTH_DISCOVERY_URL
              value: https://authentik.yanello.net/application/o/rxr/.well-known/openid-configuration
            - name: DB_DATABASE_NAME
              value: postgres
            - name: DB_HOSTNAME
              value: postgres
            - name: DB_USERNAME
              value: postgres
          envFrom:
            - secretRef:
                name: resume-secrets
          resources:
            limits:
              memory: 2048Mi
            requests:
              cpu: 1000m
              memory: 2048Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: printer
spec:
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: printer
  template:
    metadata:
      labels:
        app: printer
    spec:
      containers:
        - name: printer
          image: harbor.yanello.net/docker/chromedp/headless-shell@sha256:ba8ff0f0842e860751619b69f94184c13e134948f49fa77ec1ec15733c945484
          ports:
            - containerPort: 9222
