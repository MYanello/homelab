helmCharts:
  - name: trust-manager
    repo: https://charts.jetstack.io
    version: "v0.18.0"  # Use a current version
    namespace: trust-manager
    releaseName: trust-manager
    valuesInline:
      secretTargets:
        enabled: true
        authorizedSecrets:
          - vault-tls