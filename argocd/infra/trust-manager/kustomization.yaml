helmCharts:
  - name: trust-manager
    repo: https://charts.jetstack.io
    version: "v0.18.0"
    namespace: trust-manager
    releaseName: trust-manager
    valuesInline:
      secretTargets:
        enabled: true
        authorizedSecrets:
          - vault-tls
          - vault-trust-bundle