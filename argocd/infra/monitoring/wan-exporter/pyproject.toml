[project]
name = "wan-exporter"
version = "0.4.4"
authors = [
    { name = "Marcus Yanello", email = "marcus@yanello.net" }
]

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false

requires-python = ">=3.14"
dependencies = [
    "cryptography",
    "prometheus_client"
]