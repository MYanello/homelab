---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: adguard-exporter
  name: adguard-exporter
spec:
  selector:
    app: adguard-exporter
  ports:
    - name: metrics
      port: 9617
      protocol: TCP
      targetPort: 9617
