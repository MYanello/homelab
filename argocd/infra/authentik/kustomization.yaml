---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ./manifests/http-route.yaml
  - ./manifests/reference-grants.yaml
  - ./manifests/cnpg.yaml
  - ./manifests/backblaze-external-secret.yaml
  - ./manifests/objectstore.yaml
  - ./manifests/scheduled-backup.yaml

generators:
  - ./manifests/secret-generator.yaml

helmCharts:
  - name: authentik
    namespace: authentik
    releaseName: authentik
    repo: https://charts.goauthentik.io/
    version: "2026.5.2"
    valuesFile: ./manifests/values.yaml
