apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - https://raw.githubusercontent.com/vmware-tanzu/helm-charts/refs/heads/main/charts/velero/crds/

generators:
  - ./secret-generator.yaml

helmCharts:
  - name: velero
    releaseName: velero
    namespace: velero
    repo: https://vmware-tanzu.github.io/helm-charts
    version: 11.0.0
    # valuesInline:
    #   configuration:
    #     backupStorageLocation:
    #       - name: backblaze
    #         provider: aws
    #         bucket: myanello-velero
    #         default: true
    #         credential:
    #           name: backblaze-secrets
    #           key: applicationKey
    #         config:
    #           region: us-west-004
    #           s3Url: s3.us-west-004.backblazeb2.com
                