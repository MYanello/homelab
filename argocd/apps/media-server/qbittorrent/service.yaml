apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.version: 1.35.0 (HEAD)
  labels:
    io.kompose.service: qbittorrent
  name: qbittorrent
spec:
  ports:
    - name: "http"
      port: 8080
      targetPort: 8080
  selector:
    io.kompose.service: qbittorrent
