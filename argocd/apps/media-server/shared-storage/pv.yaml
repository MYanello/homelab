apiVersion: v1
kind: PersistentVolume
metadata:
  name: media-pv
  labels:
    type: media-storage    # Matched by PVC selector
spec:
  accessModes:
    - ReadWriteMany
  volumeMode:
    Filesystem
  nfs:
    server: marcus-server.lan
    path: /data/media