apiVersion: v1
kind: PersistentVolume
metadata:
  name: media-pv   # Matched by PVC selector
spec:
  capacity:
    storage: 10Ti
  accessModes:
    - ReadWriteMany
  volumeMode:
    Filesystem
  nfs:
    server: marcus-server.lan
    path: /data/media