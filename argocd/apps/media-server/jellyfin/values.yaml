resources:
  limits:
    nvidia.com/gpu: "1"
    cpu: "4"
    memory: 8Gi
  requests:
    nvidia.com/gpu: "1"
    
volumes:
  - name: media
    hostPath:
      path: /data/media
volumeMounts:
  - name: media
    mountPath: /data/media
      
persistence: 
  config:
    storageClass: local-path
  media:
    enabled: false

runtimeClassName: nvidia