apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: minio-cert
spec:
  commonName: minio.yanello.net
  dnsNames:
  - "minio.yanello.net"
  secretName: minio-cert
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
    group: cert-manager.io
