apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: authentik-secrets
  namespace: argocd
spec:
  destination:
    namespace: authentik
    server: https://kubernetes.default.svc
  project: default
  source:
    path: argocd/apps/authentik/secrets
    repoURL: https://github.com/myanello/homelab.git
    targetRevision: HEAD
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: authentik
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    name: in-cluster
    namespace: authentik
  project: default
  source:
    repoURL: https://charts.goauthentik.io/
    targetRevision: "*"
    chart: authentik
    helm:
      valuesObject:
        global:
          envFrom:
            - secretRef:
                name: authentik-secrets
        authentik:
          error_reporting:
            enabled: true
          env:
        server:
          ingress:
            ingressClassName: nginx
            enabled: true
            hosts:
              - authentik.k8s.yanello.net
        redis:
          enabled: true
        postgresql:
          enabled: true
          primary:
            persistence:
              enabled: true
              storageClass: nfs-storage
              accessMode: ReadWriteOnce
          auth:
            username: authentik
            existingSecret: authentik-secrets
            secretKeys:
              adminPasswordKey: AUTHENTIK_POSTGRES__PASSWORD
              passwordKey: AUTHENTIK_POSTGRES__PASSWORD
              replicationPasswordKey: AUTHENTIK_POSTGRES__PASSWORD
              