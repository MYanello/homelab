env:
  TZ: America/Chicago

resources:
  requests:
    cpu: 100m
    memory: 400Mi

persistence:
  homarrDatabase:
    enabled: true
    storageClassName: longhorn
  homarrImages:
    enabled: true
    storageClassName: longhorn
