env:
  TZ: America/Chicago

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 128Mi

persistence:
  homarrDatabase:
    enabled: true
    storageClassName: longhorn
  homarrImages:
    enabled: true
    storageClassName: longhorn
