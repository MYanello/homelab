env:
  TZ: America/Chicago

resources:
  requests:
    cpu: 150m
    memory: 800Mi
  
persistence:
  homarrDatabase:
    enabled: true
    storageClassName: longhorn
  homarrImages:
    enabled: true
    storageClassName: longhorn
