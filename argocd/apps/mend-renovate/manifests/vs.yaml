apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: renovate-vs
spec:
  hosts:
  - renovate.yanello.net
  gateways:
  - istio-system/kube-gateway
  http:
  - route:
    - destination:
        host: renovate
        port:
          number: 8080
      