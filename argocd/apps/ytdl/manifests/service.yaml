apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose -f ytdl.yaml convert
    kompose.version: 1.35.0 (HEAD)
  labels:
    io.kompose.service: ytdl
  name: ytdl
spec:
  ports:
    - name: "8998"
      port: 8998
      targetPort: 17442
  selector:
    io.kompose.service: ytdl
