---
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: crd-wizard
spec:
  endpointSelector:
    matchLabels:
      app: crd-wizard
  ingress:
    - fromEntities:
        - ingress
      toPorts:
        - ports:
            - port: "8080"
              protocol: TCP
  egress:
    - toEntities:
        - kube-apiserver
        - host
      toPorts:
        - ports:
            - port: "6443"
              protocol: TCP
