apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: config
    files:
      - ./manifests/config.toml
  - name: data
    files:
      - ./data/1984.txt
      - ./data/ai.robots.txt-robots.json
      - ./data/brave-new-world.txt
      - ./words.txt

resources:
  - manifests/sts.yaml
  - manifests/service.yaml
  - manifests/http-route.yaml
